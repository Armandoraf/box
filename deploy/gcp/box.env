# box runtime config
# Set a strong API key and keep this file root-readable only.
BOX_API_KEY=

# Optional: block media to speed up Google results.
CHROME_BLOCK_MEDIA=1

# Optional: override Chromium debug port
CHROME_DEBUG_PORT=9225
